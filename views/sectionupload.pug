h3 section信息更新
form#sectionform(enctype="multipart/form-data")
    div
        label(for="video") video:
        input#video(type="file" name="video" value="上传视频" accept="video/*")
    div
        input(type="submit" value="更新该小节")

